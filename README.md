# homebrew-redline

Homebrew tap for [redline](https://github.com/sankalpgunturi/redline) — hard time + token/$ budgets for Claude Code.

```bash
brew install sankalpgunturi/redline/redline
redline install
```
